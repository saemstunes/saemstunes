
import { FlaskConical } from "lucide-react";

// Create an aliased version of FlaskConical as Flask for compatibility
export function Flask(props: React.ComponentProps<typeof FlaskConical>) {
  return <FlaskConical {...props} />;
}

// Export other custom icons here if needed
